import Vue from 'vue';
import { shallowMount } from "@vue/test-utils";
import Login from '../atom/Login.vue';

describe('Login', () => {
    test('should mount for testing', () => {
      expect(1).toEqual(1);
    });
  });